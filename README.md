# wishup-app
Rest api creation for wishup assignment

run `npm i` to install all dependencies.

run `npm start` to start the server.

to change the host name and db settings refer to the .env file present in the base location.
